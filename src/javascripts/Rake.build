<%= include "../../HEADER" %>
<%
all_files = Dir.entries(".")
blacklist = %w{ .DS_Store Rake.build }
important_first = %w{core.js options.js template.js preloader.js trigger.js center.js effect.js}
def readfiles (files)
  output = ""
  files.each { | file |
    if File.stat(file).file?
      output << "\n\n\n\n\n/*++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

DhoniShow module: #{file}

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++*/

"
      File.open(file) { | fh |
        output << fh.readlines.to_s
      }
    end
  }
  return output
end
%>
<%= readfiles(important_first) %>
<%= readfiles(all_files-blacklist-important_first) %>